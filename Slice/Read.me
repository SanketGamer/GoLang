Slice -
1. Dynamic Size : Slices can grow or shrink dynamically during runtime.
2. Reference to Underlying Array : slice is basically under the hook Array
3. Syntax : syntax is also similar to arrays,but size is omitted,creating a slice,for example- numbers:=[]int{1,2,3}
4. use of make function : u can use the make function to create a slice with a specific length and capacity.

when we append in slice(array) a 12 val which is out of bound so then the capacity increase *2